# G3 Transport Backend Dependencies
# Node.js version: 16.x or higher

# This is a documentation file. Dependencies are managed via package.json
# Use npm commands for installation, not pip

# Core Dependencies (already in package.json):
# - bcryptjs@^2.4.3
# - cors@^2.8.5  
# - express@^4.18.2
# - sqlite3@^5.1.6

# Development Dependencies (optional):
# - nodemon@^2.0.22

# Installation Instructions:
# 1. Ensure Node.js 16.x or higher is installed
# 2. Run: npm install (installs all dependencies from package.json)
# 3. For development: npm install -D nodemon
# 4. Initialize database: npm run seed
# 5. Start server: npm start

# Note: Use npm (Node Package Manager), not pip (Python Package Installer)
